# Docker Environment Configuration for FitTrack
# This file contains the default environment variables for running FitTrack with Docker

# Flask Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production
FLASK_ENV=production

# Database Configuration (matches docker-compose.yml)
DATABASE_URL=mysql+pymysql://fittrack_user:fittrack_password@mysql:3306/fittrack
MYSQL_HOST=mysql
MYSQL_USER=fittrack_user
MYSQL_PASSWORD=fittrack_password
MYSQL_DATABASE=fittrack

# MySQL Root Password (for container setup)
MYSQL_ROOT_PASSWORD=fittrack_root_password